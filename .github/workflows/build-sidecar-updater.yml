name: Build Sidecar Updater Image

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Semantic version to label the Docker image under (no "v" prefix, e.g. "1.2.3")'
        required: true
        type: string
      tag_latest:
        description: 'Also tag this image as :latest?'
        required: false
        type: boolean
        default: false

jobs:
  build:
    name: Build sidecar-updater image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: install/sidecar-updater
          push: true
          tags: |
            ghcr.io/bigmoontech/project-nomad-wsl-sidecar-updater:${{ inputs.version }}
            ghcr.io/bigmoontech/project-nomad-wsl-sidecar-updater:v${{ inputs.version }}
            ${{ inputs.tag_latest && 'ghcr.io/bigmoontech/project-nomad-wsl-sidecar-updater:latest' || '' }}
